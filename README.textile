h1. pulsar.lua demo

This is a demo of the pulsar.lua library.

h1. Usage

Use the buttons to change modes: Set the origin (red) the destination (green) add or remove obstacles (gray).

Once an origin and a destination are set, the program will use the pulsar.lua lib to find a path between them.

